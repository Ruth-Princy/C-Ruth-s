#include<stdio.h>
int main()
{
    int n,m,i,gcd;
    scanf("%d",&m);
    scanf("%d",&n);
    if(n==0)
        gcd=m;
    if(m==0)
        gcd=n;
    for(i=1;i<=n && i<=m; i++)
    {
        if(n%i==0 && m%i==0)
        gcd=i;
    }
    printf("%d",gcd);
    return 0;
}
